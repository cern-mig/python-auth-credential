notifications:
  email:
    - mig-travis@cern.ch
language: python
python:
  - "2.5"
  - "2.6"
  - "2.7"
  - "3.2"
  - "3.3"
# command to install dependencies
install:
  - pip install pep8 --use-mirrors
# command to run tests
script:
  - pep8 auth test
  - python setup.py test
